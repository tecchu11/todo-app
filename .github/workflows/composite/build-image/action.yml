name: Build Image

inputs:
  github-token:
    description: Github token
    required: true
  build-target-module:
    description: Build target module name
    required: true

runs:
  using: composite
  steps:
    - uses: actions/setup-java@v3
      with:
        distribution: 'adopt'
        java-version: '11'
        cache: 'gradle'

    - name: Login ghcr
      shell: bash
      run: |
        echo "${{ inputs.github-token }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build Image
      shell: bash
      run: |
        echo ${{ inputs.build-target-module}}
